# Wat Prettier met rust laat.

node_modules/
dist/
dist.nieuw/
dist.oud/
coverage/
exe/public/fixtures/
package-lock.json
src/wasm/
src/puck/geometry/*.wasm

# Overgenomen uit sturnia-node: die repo bepaalt hoe dit eruitziet, niet wij.
vendor/

# De bestanden van vóór de 79/4-afspraak. Ze zijn met de hand uitgelijnd —
# de panelen in de HTML, en de taaltabel met zijn twee kolommen Nederlands en
# Engels naast elkaar — en een herformattering maakt daar gehusselde regels
# van waarin `git blame` niets meer terugvindt. De rest van src/ en de
# opmaak in exe/styles/ volgen de huisstijl wél.
exe/index.html
src/i18n/L.ts

# Uitvoer van Claude-sessies, geen broncode.
Claude outputs/

# Opnames van de tafel: door de machine geschreven, niet door ons. Ze
# herformatteren verandert honderden regels meetwaarden zonder dat er iets
# leesbaarder van wordt, en een fixture hoort te blijven zoals hij is
# opgenomen.
src/test/unit/core/fixtures/

# Naslag, geen broncode: het model waar de code aan afgemeten wordt.
resources/

# Pre-module migration sources. The maintained app and tests are in src/
# and exe/; these are kept only as the historical table snapshot.
/app.ts
/capture.ts
/globals.d.ts
/index.html
/kg.ts
/speech.ts
/styles.css
/test/
