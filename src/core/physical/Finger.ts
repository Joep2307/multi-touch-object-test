import { TangibleObject } from "./TangibleObject";

/* A bare touch: on the glass, but anonymous.
 *
 * A finger has a place and can act, and it has no identity beyond the
 * moment — put the same finger down twice and it is two different
 * physicals. That is deliberate and it is why voting was settled on
 * identified objects instead: an anonymous physical cannot be stopped
 * from voting twice, because there is nothing to stop.
 */
export class Finger extends TangibleObject {}
