export { GestureRecogniser } from "./GestureRecogniser";
export { defaultGestureDefinitions } from "./defaultGestureDefinitions";
export {
    DOUBLE_TAP_GAP_MS,
    HOLD_MIN_MS,
    PRESS_MAX_MOVE_PX,
    ROTATE_STEP_DEG,
    SHAKE_MAX_NET_PX,
    SHAKE_MIN_STEP_PX,
    SHAKE_MIN_TRAVEL_PX,
    SHAKE_REVERSALS,
    SHAKE_WINDOW_MS,
    SWIPE_MAX_MS,
    SWIPE_MIN_PX,
    TAP_MAX_MS,
} from "./constants";
export type { Gesture } from "./Gesture";
export type { GestureDefinition } from "./GestureDefinition";
export type { GestureId } from "./GestureId";
export type { GestureResult } from "./GestureResult";
