/* Two matched points fix a rigid motion in the plane completely, and
   one does not: a single point is the same point at every angle. Every
   piece of the base that matches one frame's feet against another's —
   the rotation source, the footprint completion — needs exactly this
   number, so it is stated once. */
export const MIN_MATCHED_POINTS = 2;
