/* The name a table starts a session under, and therefore the key its marks are
   stored beneath. Deliberately not translated: the language switch would then
   move the running session to a different key, and half the afternoon would
   appear to be gone. Whoever wants another name types one in the field. */
export const DEFAULT_SESSION = "session-01";
