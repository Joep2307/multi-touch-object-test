/* Three modes. `touch` and `puck` are both a table — big buttons, its own
   keyboard, windows that rotate along — but in puck mode the tray with drag
   copies is gone and only what actually lies on the glass counts. */
export type UiMode = "touch" | "laptop" | "puck";
