/* Why a capture ended. The values are the ones `wireCapture` turns into
   an explanation on screen; they are data, and stay as they were. */
export type CapReason =
    "stop" | "limiet" | "besmet" | "leeg" | "fout" | "onbruikbaar";
