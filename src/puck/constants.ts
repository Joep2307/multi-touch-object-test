/* The localStorage keys. `TPL_KEY` holds the measurements of the four
   pucks from the blueprint, `OWN_KEY` the list from the puck stand; see
   `saveTemplates` and `saveOwnPucks` for what goes in each. */
export const TPL_KEY = "pucktable-templates";
export const OWN_KEY = "pucktable-own-pucks";
